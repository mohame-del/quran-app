# تطبيق الريّان للقرآن الكريم

تطبيق React لإدارة مدرسة قرآنية مع نظام نقاط متقدم وتتبع الحضور.

## المميزات

- 🔐 **نظام مصادقة Firebase** - تسجيل دخول وخروج آمن
- 👥 **إدارة الطلاب** - إضافة وتعديل وحذف بيانات الطلاب
- 📊 **نظام النقاط المتقدم** - تتبع الحضور والعرض
- ⭐ **نظام التقييم** - 5 نجوم بناءً على الأداء
- 🔍 **البحث المتقدم** - البحث في قائمة الطلاب
- 📱 **واجهة مستخدم حديثة** - تصميم متجاوب مع Tailwind CSS

## نظام النقاط

- **الحضور الصباحي**: 1 نقطة
- **الحضور المسائي**: 1 نقطة  
- **العرض**: 1 نقطة (الأحد والثلاثاء والخميس فقط)
- **الحد الأقصى الأسبوعي**: 15 نقطة
- **التقييم**: 10/10 بناءً على النقاط
- **النجوم**: 5 نجوم بناءً على التقييم

## التثبيت والتشغيل

### المتطلبات
- Node.js (الإصدار 16 أو أحدث)
- npm أو yarn

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd elrayen-qurane
```

2. **تثبيت التبعيات**
```bash
npm install
```

3. **إعداد Firebase**
   - اذهب إلى [Firebase Console](https://console.firebase.google.com/)
   - أنشئ مشروع جديد
   - فعّل Authentication (Email/Password)
   - فعّل Firestore Database
   - انسخ بيانات التكوين

4. **تكوين Firebase**
   - افتح `src/firebase/config.js`
   - استبدل بيانات التكوين ببيانات مشروعك

5. **تشغيل التطبيق**
```bash
npm run dev
```

6. **فتح المتصفح**
   - اذهب إلى `http://localhost:5173`

## هيكل المشروع

```
src/
├── components/          # المكونات المشتركة
│   └── Navbar.jsx     # شريط التنقل
├── context/            # إدارة الحالة العامة
│   └── AppContext.jsx # سياق التطبيق
├── firebase/           # خدمات Firebase
│   ├── config.js      # تكوين Firebase
│   ├── auth.js        # خدمات المصادقة
│   └── firestore.js   # خدمات قاعدة البيانات
├── pages/              # صفحات التطبيق
│   ├── Login.jsx      # صفحة تسجيل الدخول
│   ├── Register.jsx   # صفحة التسجيل
│   ├── Dashboard.jsx  # لوحة التحكم
│   ├── Students.jsx   # قائمة الطلاب
│   ├── AddStudent.jsx # إضافة طالب
│   ├── Evaluation.jsx # التقييم
│   ├── StudentProfile.jsx # ملف الطالب
│   └── MySchool.jsx   # معلومات المدرسة
└── main.jsx           # نقطة البداية
```

## الصفحات والوظائف

### 1. تسجيل الدخول والتسجيل
- تسجيل حساب جديد
- تسجيل الدخول بحساب موجود
- إدارة حالة المصادقة

### 2. لوحة التحكم
- عرض إحصائيات الطلاب
- ترتيب الطلاب حسب النقاط
- نظرة عامة على الأداء

### 3. قائمة الطلاب
- عرض جميع الطلاب
- البحث في الطلاب
- تسجيل الحضور (صباح/مساء)
- تسجيل العرض (أحد/ثلاثاء/خميس)
- تحديث بيانات الحفظ

### 4. إضافة طالب
- إدخال البيانات الأساسية
- تحديد الحزب والثمن
- رقم هاتف الأب

### 5. التقييم
- عرض تقييم الطلاب
- إضافة ملاحظات
- نظام النقاط والنجوم

### 6. ملف الطالب
- عرض تفاصيل الطالب
- سجل الحضور والعرض
- تحديث بيانات الحفظ
- عرض الملاحظات

## قاعدة البيانات (Firestore)

### مجموعة الطلاب (students)
```javascript
{
  id: "string",
  firstName: "string",
  lastName: "string", 
  hizb: "number",
  quarter: "number",
  fatherPhone: "string",
  totalPoints: "number",
  weeklyPoints: "number",
  monthlyPoints: "number", 
  yearlyPoints: "number",
  attendanceHistory: "array",
  presentationHistory: "array",
  weeklyRating: "number",
  monthlyRating: "number",
  yearlyRating: "number",
  stars: "number",
  notes: "array",
  createdAt: "timestamp",
  updatedAt: "timestamp"
}
```

### مجموعة التقييمات (evaluations)
```javascript
{
  id: "string",
  studentId: "string",
  rating: "number",
  notes: "string",
  createdAt: "timestamp"
}
```

## النشر

### Netlify
1. اربط مستودع GitHub بـ Netlify
2. اضبط build command: `npm run build`
3. اضبط publish directory: `dist`

### Vercel
1. اربط مستودع GitHub بـ Vercel
2. سيتم التعرف التلقائي على إعدادات React

### Firebase Hosting
1. ثبت Firebase CLI: `npm install -g firebase-tools`
2. سجل دخول: `firebase login`
3. ابدأ المشروع: `firebase init hosting`
4. بني المشروع: `npm run build`
5. انشر: `firebase deploy`

## استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ في استيراد Firebase**
   ```bash
   npm install firebase
   ```

2. **خطأ في التكوين**
   - تأكد من صحة بيانات Firebase
   - تحقق من تفعيل Authentication و Firestore

3. **مشاكل في العرض**
   - امسح cache المتصفح
   - أعد تشغيل خادم التطوير

4. **مشاكل في البيانات**
   - تحقق من قواعد Firestore
   - تأكد من صلاحيات المستخدم

## المساهمة

1. Fork المشروع
2. أنشئ فرع جديد: `git checkout -b feature/new-feature`
3. اكتب التغييرات: `git commit -am 'Add new feature'`
4. ادفع الفرع: `git push origin feature/new-feature`
5. أنشئ Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

## الدعم

للمساعدة والدعم:
- افتح Issue في GitHub
- تواصل مع المطور عبر البريد الإلكتروني

## التحديثات القادمة

- [ ] نظام إشعارات
- [ ] تقارير مفصلة
- [ ] تطبيق موبايل
- [ ] نظام رسائل
- [ ] دعم متعدد اللغات 